#!/bin/bash -e

#echo "---> Installing application source"
#cp -Rf /tmp/src/. ./
ls -l /usr/bin/sudo
ls -l /etc/sudoers
sudo su -
#adduser --shell /bin/bash --system --ingroup root --force-badname --uid 1005790000 x3193
#echo "x3193 ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
#usermod -a -G sudo x3193
#usermod -a -G adm x3193

#service ssh start 
#sudo service ssh start
#exit

#semanage login -a -s staff_u ops
#semanage login -a -s root ops
cat /etc/sudoers
#sudo cat /etc/sudoers
id -un
cat /etc/group
echo "20000000"
#sudo ls
expect /exp.sh
cat /etc/pam.d/su


